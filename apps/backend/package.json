{
  "name": "backend",
  "version": "1.0.0",
  "description": "RAG_GOD Backend Service for Goldngoose",
  "private": true,
  "scripts": {
    "dev": "cd ../rag_god && python src/main.py",
    "start": "python main.py",
    "install-deps": "pip install -r requirements.txt",
    "test": "pytest tests/"
  }
}
